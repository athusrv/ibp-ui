import { SocialFrontCardData } from './social-front-card-data';

describe('SocialFrontCardData', () => {
  it('should create an instance', () => {
    expect(new SocialFrontCardData()).toBeTruthy();
  });
});
